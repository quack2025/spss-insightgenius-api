# Core Framework
fastapi>=0.135.0
uvicorn[standard]>=0.27.0
gunicorn>=21.2.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Data Processing
pyreadstat>=1.2.6
pandas>=2.1.4
numpy>=1.26.3
scipy>=1.12.0
statsmodels>=0.14.1

# Market Research Engine (QuantipyMRX fork)
# In Docker: installed separately in Dockerfile (pinned commit)
# For local dev: pip install quantipymrx @ git+https://github.com/quack2025/QuantiyFork2026.git@main
quantipymrx @ git+https://github.com/quack2025/QuantiyFork2026.git@main

# AI (Haiku for ticket parsing + smart labeling)
anthropic>=0.18.0

# File parsing
python-docx>=1.1.0
openpyxl>=3.1.2
python-pptx>=0.6.23  # required by quantipymrx (PowerPoint export module)

# Export formats
pyarrow>=14.0.0

# Distributed rate limiting & caching (optional — falls back to in-memory)
redis>=5.0.0

# MCP (Model Context Protocol) server — SSE transport
fastmcp>=2.0.0

# Auth (Clerk JWT validation + Supabase JWT)
PyJWT[crypto]>=2.8.0

# HTTP
httpx>=0.26.0

# Database (PostgreSQL — optional, enables project/conversation features)
sqlalchemy>=2.0.25
alembic>=1.13.1
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# Encryption (optional — SPSS files at rest)
cryptography>=41.0.0

# Retry logic
tenacity>=8.2.3
